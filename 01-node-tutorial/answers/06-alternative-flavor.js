/*function alternative (name) {
    console.log(`Printing ${name} from 06-alternative-flavor.js`);
}
module.exports = alternative */

const people = {
    karen: 'Karen',
    billy: 'Billy',  
};

module.exports.items = people